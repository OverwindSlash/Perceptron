# IVideoLoader 规格文档索引

本文档已由正式规格 [VideoLoader-Spec.md](./VideoLoader-Spec.md) 取代。

后续关于 `IVideoLoader`、`MediaLoader.OpenCV`、未来 `MediaLoader.FFMpeg` 后端、`OpenCvSharp.Mat` 输出契约、状态机、资源所有权和验收标准的维护，请统一更新 `VideoLoader-Spec.md`。
